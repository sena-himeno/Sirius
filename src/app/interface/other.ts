export interface ArchiveConfigType {
    todo: boolean
    diary: boolean
    mood: boolean
    chart: boolean
    defaultPreMonth: boolean
    selectMonth: string
    fullYear: boolean
    via: string
}